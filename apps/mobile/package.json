{
  "name": "@opentreasury/mobile",
  "version": "0.0.0",
  "private": true,
  "description": "Placeholder workspace. The Expo app is scaffolded in blueprint Phase 6.7; dependencies are intentionally empty until then so the stub carries no supply-chain surface.",
  "scripts": {
    "start": "echo 'The mobile app is not scaffolded yet — see docs/opentreasury-blueprint.md Phase 6.7' && exit 1"
  }
}
